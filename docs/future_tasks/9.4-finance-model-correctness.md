# 9.4 Finance-model correctness & honest labels

> **✅ DONE — implemented in spec 027 (2026-07-18), branch `feat/finance-model-correctness`.**

- **Section:** 9. Commercial readiness
- **Priority:** 🔴 high (verifications) · 🟡 medium (labels/policy)
- **Track:** A (correctness) + small B (labeling)
- **Verifies against:** the §9.1 coverage corpus

A grouped set of correctness/trust items surfaced in the readiness review.

- **A2 — Timezone insight-bucketing** 🔴 ✅ *Confirmed bug; fixed in spec 027.*
  `inInterval` now calls `parseLocalDate` for date-only strings so both sides of
  the comparison use the same local-midnight regime as `monthInterval`. Non-UTC
  regression test added: `web/test/insights-timezone.tz.test.ts`.
- **A4 — CLI leftover-cent divergence** 🟡 ✅ *Verified NO divergence in spec 027.*
  CLI already calls `orderedOwnerIds` before `computeShares`; `sort_order` does not
  affect the leftover cent. PARITY.md updated with verification note and test:
  `web/test/import/toTransaction.test.ts` "A4 — sort_order ≠ UUID order".
- **A3 — Extend the independent oracle** 🟡 ✅ *Done in spec 027.*
  `web/test/finance-goldens.test.ts` now covers amortization schedule, insights
  budget-over and recurring-truncation rules, `monthBounds` filter windows, and
  `daysUntilNextRent` month-end clamping. `web/test/finance-properties.test.ts`
  extended with 4 insights invariants (length, id patterns, magnitude ≥ 0, severity sort).
- **B3 — Honest labels for approximations** 🟡 ✅ *Done in spec 027.*
  "Equity" → "Principal paid down" (all housing surfaces); "Net rental income" →
  "Net rental (P&I only)"; `PAID_OFF_THRESHOLD_CENTS = 500` display guard added to
  mortgage.ts and used in MortgageCards, HousingSnapshotCard, HousingDesktop,
  DashboardDesktop. i18n catalogs updated for all 5 locales.
- **B4 — Rounding-cent fairness policy** ⚪ ✅ *Done in spec 027.*
  Policy comment added above the leftover distribution loop in `web/lib/splits.ts`.
  PARITY.md "Canonical leftover-cent order" row updated with policy reference.

> Source: the commercial-readiness & sequencing review (2026-07-17).
