# 4.4 Transaction tags & richer notes

- **Section:** 4. Money management
- **Priority:** ⚪ low / exploratory

- **What:** Free-form tags (orthogonal to category) + notes/attachments.
- **How it works:** a `tags` table + join; filter/report by tag.
- **Ortho fit:** small, additive; complements our existing filters.

> **✅ Delivered in `specs/027-transaction-tags/` (2026-07-18).** Shipped the `tags` +
> `transaction_tags` join and a nullable `transactions.notes` column, an inline tag editor on the
> transaction form, and a tag dimension + notes/tag-name search in the existing filter/search stack
> (vector-locked in `transaction-filters.json`). Deferred for a possible follow-up: file/photo
> **attachments** (v1 notes are plain text), tag rename/merge management, and a dashboard
> "spend by tag" breakdown (reporting is via the filter surface for now).
