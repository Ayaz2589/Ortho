{
  "name": "@ortho/aggregation-core",
  "version": "0.1.0",
  "private": true,
  "description": "Extraction-ready bank-aggregation core: provider-agnostic types + the Plaid REST request builders, response parsers, and error mapping behind the plaid-* edge functions. Pure runtime-agnostic TypeScript — zero runtime dependencies, zero imports from the host app.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "sync:functions": "node scripts/sync-to-functions.mjs"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5",
    "vitest": "^4.1.8"
  }
}
