import type { LandingCatalog } from './index'

const bn: LandingCatalog = {
  metaTitle: 'Ortho — ঘরের হিসাব, গোছানো।',
  metaDescription:
    'খরচের হিসাব রাখা, ভাগ করে নেওয়া আর একসাথে পরিকল্পনা করার একটি শান্ত উপায়। পরিবারের জন্য, আপনার ভাষায়।',
  placeholderLine: 'ঘরের হিসাব, গোছানো।',
}

// --- spec 046 landing copy — insert only between these markers ---
// --- end spec 046 ---

// --- spec 047 tour copy — insert only between these markers ---
// --- end spec 047 ---

export default bn
