# Archived backlog items — shipped records

Completed backlog items, moved here once fully shipped (each file keeps its ✅
status banner naming the spec/PR that delivered it). The **active** backlog is
[`../index.md`](../index.md); partial items (e.g. §1.1, §4.1, §5.1) stay active
there until the remaining slices ship.
